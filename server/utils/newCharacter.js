const { Character } = require('../models/Character')

async function newCharacter (data) {
    console.log('i ran')
    // const createNewCharacter = await Character.create(args)
}

module.exports = newCharacter